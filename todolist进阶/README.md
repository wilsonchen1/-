## 过程
### 1.思路
- 代码是根据我上次的作业，进行的语法改正，其中删除操作 和 修改finished的函数用了参考了demo的方法，因为之前作业用了e.target.parentnode，但是我没有查到ts有类似用法，打包之后删除操作异常，所以用了demo里的方法。
- 另外修改了上一次作业的逻辑，把事件绑定放在了创建节点的函数中，避免了出现多次添加todo之后删除和已做按钮失灵。（原作业把事件绑定在了init()初始化函数，导致新添加的节点有可能事件失灵。
### 2.遇到的问题
- 点击已做按钮时，修改时间的功能只有刷新页面之后才能显示，因为要调用init()才能读取刷新之后的数据，不知道有没有好方法可以及时的显示（也许需要重新写节点？）

- 查阅了非常多的文档，webpack遇到了很多问题，包括路径不对，依赖包没有安装，配置文件错误，还意识到了相对路径的问题，由于在html中把路径写死了，导致打包一直不成功，后来才发现。
- 关于dayjs库，我找的是英文版本，所以加上css文件之后会把行高变大，但是可以显示时间。（dayjs这个库我尝试多次打开，其中有一次出现了时间显示undifined的情况，不太清楚是不是代码逻辑的问题）
- 在编译之后，遇到了 html语法报错的问题，后面经过老师的耐心教导，了解到sourcemap的用处，可以起到定位的作用。